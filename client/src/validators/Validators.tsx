/**
 * Validators destination list (P1-10).
 * Profile at `#/validators/:address` is owned by P1-11 (`Profile.tsx`).
 */
import Directory from './Directory'

export default function Validators() {
  return <Directory />
}
