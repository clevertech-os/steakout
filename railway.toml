[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "node --import tsx server/src/index.ts"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
