# Test Layout

- `unit/client/`: client unit tests
- `unit/server/`: server unit tests
- `integration/`: future API and persistence integration tests
- `fixtures/`: sanitized chain and registry fixtures owned by later spikes
